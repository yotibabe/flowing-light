import os
import glob

css_path = 'css/style.css'

fancy_css = """
/* =========================================
   FANCY INTERACTIVE EFFECTS (NON-INVASIVE)
   ========================================= */

/* 1. Custom Magnetic Cursor */
@media (pointer: fine) {
    body { cursor: none; }
}

.fancy-cursor {
    position: fixed; top: 0; left: 0; width: 20px; height: 20px;
    border: 1.5px solid rgba(166, 58, 25, 0.8); border-radius: 50%;
    pointer-events: none; z-index: 999999; transform: translate(-50%, -50%);
    transition: width 0.3s ease, height 0.3s ease, background-color 0.3s ease;
}

.fancy-cursor-dot {
    position: fixed; top: 0; left: 0; width: 4px; height: 4px;
    background-color: #a63a19; border-radius: 50%;
    pointer-events: none; z-index: 1000000; transform: translate(-50%, -50%);
}

.fancy-cursor.hovering {
    width: 60px; height: 60px; background-color: rgba(255, 255, 255, 0.2);
    border-color: rgba(166, 58, 25, 0import os
import glob

css_path = 'css/style.css'

fanilimplement 
css_path sit
fancy_css = """
/* =============nsf/* ===========se   FANCY INTERACTIVE EFFECTS (NON-INVAS. Smoo   ====================================y: 0;
/* 1. Custom Magnetic Cursor */
@media (pointopa@media (point-bezier(0.22, 1, 0.    body {ansform 1s cubi}

ezier(0.22, 1, 0.36, 1);
}
    position: is    border: 1.5px solid rgba(166, 58, 25, 0.8); border-radius: An    pointer-events: none; z-index: 999999; transform: translate(-5,     transition: width 0.3s ease, height 0.3s ease, background-color 0.3s ea
 }

.fancy-cursor-dot {
    position: fixed; top: 0; left: 0; width: 4px; heigm: tr    ate(0px, 0px) sc    background-color: #a63a19; border-radius: 50%;
    pointeou    pointer-events: none; z-index: 1000000; trans}

.fancy-cursor.hovering {
    width: 60px; height: 60px; background-color:not i    width: 60px; height o    border-color: rgba(166, 58, 25, 0import os
import glob

css_path = '_jimport glob